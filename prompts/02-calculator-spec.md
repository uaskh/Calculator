# Prompt 2: the calculator specification

Given as `/spec calculator` with these notes, after the project folder existed. The output
is `specs/calculator.md`. The decisions that `/spec` would otherwise have asked about are
written into the notes, so a rerun needs no questions.

---

/spec calculator

its for a senior backend engineer assessment, they gave a brief (keep the brief itself in docs/brief.md, git ignored, ci should fail if its ever committed, and record the prompts used in docs/prompts.md). build a web calculator: react + typescript spa, go rest microservice. user types or taps an expression, frontend sends it to backend, backend tokenizes parses into an AST and evaluates and returns json. production grade, explicit errors, tests every layer, clear docs, no over engineering. no eval no regex arithmetic, must be a real lexer parser ast evaluator.

operators: + - * / with normal precedence and left assoc, decimal numbers, parentheses, unary minus (-3, 2*-3, -(2+3), --3). the optional ones from the brief i want all in scope: ^ right assoc and above unary minus so -2^2 = -4 and 2^-1 = 0.5 and 2^3^2 = 512, sqrt(x), and postfix % with calculator semantics: x% is x/100 except when the % node is the direct right operand of + or - then its L*x/100 so 200+10% = 220 and 200-10% = 180 but 200+(10%) = 200.1 and 200+10%*2 = 200.2, unary minus breaks it (200+-10% = 199.9, -50% = -0.5), repeated % only outermost gets the rule (100+50%% = 100.5), group inside the % node doesnt break it (200+(10)% = 220), % binds tighter than ^ (2%^2 = 0.0004, 2^2% = 2^0.02). keep parens in the ast as a group node because they change % meaning. give the grammar explicitly. leading zeros ok (007 = 7). lexer takes number as maximal run of digits and dots so 1.2.3 is one INVALID_NUMBER token at 0. identifiers maximal ascii letters, anything but sqrt is UNKNOWN_FUNCTION, sqrt case sensitive. whitespace only space and tab between tokens, anything else incl newline is INVALID_CHARACTER, only ascii operators (× ÷ − rejected, keypad inserts * / -). implicit multiplication and unary plus and () are UNEXPECTED_TOKEN.

lenient normalization on the token stream before parsing because of live typing: trim, repeatedly drop trailing binary operator or trailing dot or trailing ( or sqrt( pair (with whitespace before it), close unmatched (, empty after that = EMPTY. return the evaluated expression string back so 2*(3+4 gives expression "2*(3+4)" result 14, 3+4* gives "3+4" 7, 2. gives "2", sqrt( is EMPTY, 2.. and 2.5. are INVALID_NUMBER only single trailing dot repairable, spacing preserved otherwise. error positions always index the original untrimmed input 0 based ( 2 3 → position 4). validation order fixed and stops at first: TOO_LONG → lex errors → normalization → EMPTY → TOO_DEEP → parse errors so errors array always has exactly one entry.

numbers: no floats, exact decimals with github.com/shopspring/decimal (thats the one allowed module, i know the harness says stdlib only, the first implement phase should relax that with my approval: hooks.conf policy, CLAUDE.md wording, backend rules, definition of done, depguard). literals parsed exact then rounded to 32 decimal places half away from zero same as every intermediate so nothing has more than 100 integer digits + 32 decimals. + - * % and integer ^ exact when result fits 32 places, negative integer exponent as (1/x)^|n| through the 32 place division path (3^-1 = 0.3333333333333333, 2^-1000 = 0, 0.5^-1000 = RESULT_TOO_LARGE). inexact ops / fractional ^ sqrt computed with at least 40 guard digits (newton for sqrt, library power with precision for fractional ^) then rounded to 32 so 1/3*3 = 1 and sqrt(2)*sqrt(2) = 2. integer powers by square and multiply with each partial rounded to 32 and rejected the moment it hits 10^100. pre check before x^n: reject when (d-1)*floor(|n|) >= 100 with d integer digits of |x| so (1.1^1000)^1000 and 99^999.5 fail without computing. final result rounded to 16 places. results are json strings canonical form no trailing zeros no exponent no -0, regex ^-?(0|[1-9][0-9]*)(\.[0-9]*[1-9])?$ goes in openapi. errors all 422: DIVISION_BY_ZERO (also 0^negative, 5/0%, 10^99/10^-99 because 10^-99 rounds to 0), NEGATIVE_SQUARE_ROOT, INVALID_POWER negative base non integer exponent, EXPONENT_TOO_LARGE |exponent| > 1000 checked before computing, RESULT_TOO_LARGE any literal intermediate or final |value| >= 10^100 (10^100/10^100 is an error even though the result would be small). 0^0 = 1. check order for ^: EXPONENT_TOO_LARGE, DIVISION_BY_ZERO, INVALID_POWER, RESULT_TOO_LARGE so (-8)^1001.5 is EXPONENT_TOO_LARGE. exponents are evaluated values so (-2)^(1/3*3) is INVALID_POWER, document that in readme. limits: 1024 unicode code points before normalization TOO_LONG, depth 32 counting ( and sqrt( only TOO_DEEP, body 4 KiB 413, handler timeout 5s 503 TIMEOUT, all constants in readme. put lots of concrete examples as acceptance criteria for every rule (i want the exact strings: 2+3*4 → "14", 0.1+0.2 → "0.3", 1/3 → "0.3333333333333333", 2/3 → "0.6666666666666667", 2^100 full digits, 2^0.5 → "1.414213562373095", 2^2% → "1.0139594797900291", 0.1^20 → "0", -0.00000000000000005 → "-0.0000000000000001", 1 followed by 100 zeros → RESULT_TOO_LARGE but 0 followed by 100 zeros → "0", 10^99 ok 10^100 not, 2^300 ok, 1.0001^1000 ok 1.0001^1001 EXPONENT_TOO_LARGE, and so on for every code and position).

api: base /api/v1, single POST /api/v1/evaluate with {"expression": "..."} (thats the whole api, keep it simple), 200 {"expression": normalized, "result": string}. errors rfc 9457 problem details per the api rules: 400 MALFORMED_REQUEST for bad json wrong type unknown field multiple values, 400 VALIDATION_FAILED with errors [{field expression, code one of REQUIRED EMPTY TOO_LONG TOO_DEEP INVALID_CHARACTER INVALID_NUMBER UNEXPECTED_TOKEN UNBALANCED_PARENTHESIS UNKNOWN_FUNCTION, position 0 based (omitted for REQUIRED EMPTY TOO_LONG TOO_DEEP), message}] and detail repeats the message, null expression is REQUIRED non string is MALFORMED, 413, 415 (parse with mime.ParseMediaType so charset param ok), 422 codes above with detail = the ui sentence, 503 TIMEOUT, 503 NOT_READY on readyz during shutdown, 404 405 with Allow, 500 INTERNAL_ERROR. fixed message templates with N = position+1: expression is required / expression is empty / expression exceeds 1,024 characters / expression is nested deeper than 32 levels / invalid character '$' at character N / invalid number at character N / unknown function 'foo' at character N / unexpected ')' at character N or unexpected end of input (position = input length) / unbalanced ')' at character N. every response X-Request-ID (32 lower hex from crypto/rand, echo client value matching ^[A-Za-z0-9._-]{1,64}$) and Cache-Control no-store. security headers on backend and nginx: X-Content-Type-Options nosniff, X-Frame-Options DENY, Referrer-Policy no-referrer, Permissions-Policy camera=(), microphone=(), geolocation=(), Cross-Origin-Opener-Policy same-origin, html shell also CSP default-src 'self'. cors off unless CORS_ALLOWED_ORIGINS exact list, * rejected at startup, preflight GET, POST / Content-Type, X-Request-ID / max age 600. healthz and readyz, readyz 503 once shutdown started, flag flips on sigterm before Shutdown runs.

ui: single screen mobile first from 320px. header title Calculator, document title Calculator. labelled expression input maxLength 1024 autocomplete off spellcheck false, inputMode none on touch devices via (pointer: coarse) so the keypad is the input and soft keyboard stays hidden, text otherwise. live result in an output aria-live polite below it, both monospace single line horizontal scroll so a 118 char result is never cut. "Evaluated as ..." muted when normalized expression differs from trimmed input, only on 200, hidden on error and after commit. live result 150ms after last change, exactly one request, abort in flight on change, stale never shown, 400 EMPTY shown as blank not error, previous result stays while pending and "Calculating…" replaces it only if no response after 300ms. message region: while typing status text polite, on commit role alert + aria-invalid + aria-describedby, input preserved, any edit clears the alert. keypad 4 column grid rows C ⌫ ( ) / 7 8 9 ÷ / 4 5 6 × / 1 2 3 − / 0 . % + / sqrt ^ = with = type submit spanning two columns, aria labels clear backspace open parenthesis close parenthesis divide multiply subtract add decimal point percent square root power equals, sqrt inserts sqrt(, every key appends at end caret at end keeps focus on input, insert over 1024 ignored, every key >= 44x44 measured in playwright and reachable by tab. enter or = commits with a fresh request (cancel debounce, abort live, always send), result replaces input with negatives wrapped as (-5) so ^2 after works, normalized expression = raw result added to history, result area blank until next edit, = disabled and enter ignored while committing, enter on empty does nothing, committed EMPTY leaves input blank result no history, every commit appends duplicates included, focus stays on input. C or escape clears expression result error aborts in flight keeps focus, ⌫ removes one char (sqrt( → sqrt). history: heading History, ul of buttons "2+2 = 4" newest first max 20 in memory only, hidden when empty, activating loads the expression caret at end focus back and live result through the normal debounce, reload empties it. wording: DIVISION_BY_ZERO Cannot divide by zero. NEGATIVE_SQUARE_ROOT Cannot take the square root of a negative number. INVALID_POWER Cannot raise a negative number to a fractional power. EXPONENT_TOO_LARGE Exponent must be between -1000 and 1000. RESULT_TOO_LARGE Result is too large to calculate. validation → server message verbatim, network / 5xx / 10s client timeout → The calculator service is unavailable. Try again. anything else or unknown code → Something went wrong. Try again. aria-invalid only for 400 and 422 not network. theme honours prefers-color-scheme with >= 4.5:1 contrast asserted by axe in playwright and a unit test over the tokens, any transition disabled under reduced motion.

nfrs: wcag 2.2 AA zero axe violations on both playwright projects (Desktop Chrome 1280x720 and Pixel 7), no page horizontal scroll at 320 412 1280, coverage 80% per side 90% domain with docs/coverage.md, parsing single pass and a go benchmark over a named worst case corpus (1024 char literal, 1.000…01^1000 chains, 2^0.5 and sqrt(2) chains, 32 deep nesting, largest legal integer power) each under 5ms with numbers in coverage.md, structured slog per request with request_id method path status duration_ms bytes remote_addr user_agent and error on 5xx/panics, panic before write → 500, config env with defaults HTTP_ADDR :8080 LOG_LEVEL info LOG_FORMAT json (dev script uses text) HTTP_SHUTDOWN_TIMEOUT 10s CORS_ALLOWED_ORIGINS empty, server timeouts read header 5s read 10s write 15s idle 60s max header 1MiB, VITE_API_BASE_URL empty same origin with /api/v1 appended, vite proxy /api → localhost:8080. maintainability: adding a binary operator = one entry in an operator table (token precedence associativity evaluate func), adding a function = one entry in a function table, % and ^ are entries whose functions own their special rules, no switch on operator tokens outside the domain package.

deliverables: git repo with backend frontend Makefile scripts/dev.sh, readme per the doc rules with executed curl examples, openapi.yaml, tests every layer, docs/coverage.md real numbers, adrs for dependency policy api shape and error model number representation lenient normalization percent semantics live preview strategy container topology, docs/prompts.md, dockerfiles with -healthcheck flag, compose.yaml with web on 3000 (nginx serving spa proxying /api/) and backend on 8080 published, github actions ci running make verify and compose smoke test.

write all of this into the spec with ids and acceptance criteria, put every decision above into the decisions table so implement doesnt have to ask, and if anything is still ambiguous ask me now not later.
