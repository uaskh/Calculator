# Prompt 1: set up the Claude Code project folder

Given to Claude Code in an empty folder, before anything else existed. The output is the
`.claude/` directory (engineering guide, rules, agents, skills, hooks, settings) and the
`specs/` folder with its template.

---

i want you to setup this repo as a claude code project for building full stack apps. backend is go rest microservice, frontend is react + typescript spa with vite. make a .claude folder with everything in it so that later i can just write a spec and run /implement and it builds the whole thing properly like a senior team would, with tests and docs and docker and ci. dont build any app now, only the tooling.

here is what i want inside .claude

**CLAUDE.md** – the engineering guide, short, non negotiable rules. specs are source of truth, if spec is unclear stop and ask me with AskUserQuestion with options and recommended one first, never invent product behaviour. backend go standard library only, net/http with method patterns and wildcard servemux, encoding/json, log/slog, no frameworks no routers no orm no di no assertion libs, go.mod has no require block, dev tools like golangci-lint govulncheck are binaries not deps and hooks should enforce this. frontend react + strict typescript on vite, css modules and css custom properties, no ui kit no css framework no state library, runtime deps only react and react-dom and anything else needs justification and my approval. tests ship with the code, test first red green refactor, every acceptance criteria has a test, layers unit component http handler black box api and browser e2e, coverage 80% per side and 90% for domain logic, never skip or delete or weaken tests or lower thresholds. SOLID with clean boundaries, domain pure and framework free, transport depends on domain never reverse, dependencies injected via small interfaces owned by consumer and wired only in composition root, new behaviour by registration not editing switches everywhere. production quality by default: validated input, rfc 9457 problem details errors, timeouts, graceful shutdown, structured logs, health checks, config via env with safe defaults, security headers, no secrets, accessible wcag 2.2 AA and responsive. docs are part of done, readme stays accurate and every command in it has been run, ADRs in docs/adr. no TODOs no commented out code no placeholder text no magic values. also put the repo layout in it (backend/cmd/<service>, internal/app, internal/config, internal/<domain>, internal/httpapi, internal/server, api/openapi.yaml, test/e2e; frontend/src/api, features/<name>, components, styles, test, e2e; specs; docs; Makefile) and the make commands table (setup dev run-backend run-frontend fmt fmt-check lint typecheck test test-e2e coverage build vuln verify docker-up docker-down), the workflow (/spec, /implement, /verify, /review, /readme), the subagents list, git rules (commit at end of each phase automatically with conventional commits, never push force amend rebase, format on commit through githooks, never commit secrets env node_modules build output coverage) and the hooks description and a definition of done.

**rules/** folder with path scoped rules (frontmatter with paths glob) so they only load when matching files exist: api-contract.md (versioned base /api/v1, healthz readyz unversioned, json only 415 otherwise, body limit 413, camelCase fields, unknown fields 400, rfc 9457 problem shape with stable UPPER_SNAKE code and errors array, status table 400 vs 422 split, every response X-Request-ID, cors off by default with CORS_ALLOWED_ORIGINS allow list, additive changes only within v1, when contract changes update handler openapi frontend types e2e readme in same commit), backend-go.md (dependencies, layering table with what each package may import, http layer rules decode validate call domain map encode, decodeJSON with content type check max bytes disallow unknown fields single value, writeJSON marshal before write, one error mapping function, middleware order request id → access log → recover → security headers → cors → timeout → mux, server timeouts, config only from env via config.Load(getenv), thin main with testable run, errors wrapped with %w, slog only, no goroutines without owner, race detector, hostile input list, style with gofmt goimports golangci-lint v2 and doc comments), frontend-react.md (stack, structure, typescript strict with noUncheckedIndexedAccess noImplicitOverride exactOptionalPropertyTypes, no enums no any, state local first useReducer exported and tested, all http via src/api with createHttpClient injected through context useApi, every request cancellable abort on unmount ignore stale, map errors by kind/code, forms with labels aria-invalid aria-describedby role alert live regions, a11y and responsive from 320px with 44px targets, css modules with tokens no inline no important, error boundary, config via import.meta.env in src/config.ts, eslint type aware + jsx-a11y + prettier zero warnings), testing.md (layers table, test first, table driven go tests with t.Parallel and t.Context, fakes not mocks, fuzz targets for parsers, frontend query by role, msw at http boundary, playwright role locators web first assertions no waitForTimeout desktop + mobile projects, coverage rules and thresholds, determinism no sleeps), delivery.md (makefile compatible with gnu make 3.81, multi stage docker with distroless static go binary non root healthcheck and nginx-unprivileged for frontend with spa fallback and security headers, compose waits on health, ci github actions mirroring make verify with least privilege and current action versions, gitignore editorconfig, shell scripts set -euo pipefail), documentation.md (what readme must contain: overview features, architecture mermaid, tech stack versions, quick start docker, run each side, config table, api reference with executed curl examples and responses, testing and coverage numbers, project structure, design decisions adr links assumptions limitations next steps, troubleshooting; adr format; openapi rules).

**agents/** – subagent definitions with name description tools and detailed instructions: spec-analyst (read only, requirements matrix with testable acceptance criteria, ambiguities as ready to ask questions with options, conflicts, edge cases, low risk assumptions, also a compliance mode to audit implementation against spec), go-backend-engineer (implements a well specified slice test first stdlib only, returns summary files tests commands run deviations open questions, cannot ask me questions), react-frontend-engineer (same for frontend), e2e-test-engineer (black box api tests + playwright journeys), code-reviewer (read only apart from running checks, focus backend frontend or delivery, evidence backed findings with severity), bug-hunter (adversarial, proves bugs with executable probe tests that it deletes afterwards, reports only reproduced bugs), docs-writer (turns a fact packet into readme adrs openapi descriptions and checks every command it documents). subagents cant talk to me so they return open questions and the main agent asks.

**skills/** as slash commands:

- spec: /spec <name> [notes or file] → writes specs/<name>.md from a spec template (summary, goals non goals, users, functional requirements table with ids and priorities, acceptance criteria given when then, business rules and edge cases, api requirements, ui requirements, nfr table, constraints, deliverables, decisions table, open questions), never invents behaviour, runs spec-analyst for long briefs, asks me the open questions max 4 per call recommended first, writes answers into the decisions table.
- implement: /implement <name> [--from phase] [--only backend|frontend] with phases 0 resolve spec and inspect repo (toolchain check, greenfield vs brownfield vs resume), 1 analyse and clarify with spec-analyst and ask me everything blocking, 2 plan written to specs/<name>.plan.md from a plan template (requirements matrix, decisions and assumptions, architecture, api contract first, ui design, config, test strategy, tasks with checkboxes per phase, dependencies to add, risks, progress log) and ask me to approve then commit, 3 scaffold greenfield with a script that copies a verified skeleton (repo files, backend skeleton, frontend overlay on top of vite react-ts) and installs dev deps, 4 backend test first via go-backend-engineer with a subagent brief template, 5 frontend in parallel, 6 integration and e2e incl checking the running product myself with curl and screenshots and docker compose smoke, 7 verify review adrs readme, 8 report against a definition of done. include the references (clarification guide of what to always ask vs decide as assumption, plan template, scaffold guide, backend feature pattern with a full worked vertical slice, frontend feature pattern, definition of done and final report template) and the templates: a verified go skeleton (cmd/api main with testable run and -healthcheck flag, config from env with all errors joined, server with timeouts and graceful shutdown, httpapi router with problem 404/405, strict decode, problem details, middleware, health endpoints, e2e test, golangci v2 config with depguard stdlib only, dockerfile distroless, openapi), a frontend overlay (typed http client with abort timeout and problem parsing, ApiProvider useApi, error boundary, config, tokens.css global.css, msw setup, render helper, playwright config with desktop and pixel 7 projects starting both apps on dedicated ports, vitest config with 80% thresholds and json summary, nginx conf, dockerfile, env example, prettier) and repo files (Makefile with all targets and help, ci.yml with backend frontend e2e docker jobs, docker-compose, gitignore, editorconfig, coverage-report.sh that writes docs/coverage.md and enforces thresholds, dev.sh running both servers).
- verify: /verify [quick|full] runs the gates in order (fmt, lint, typecheck, test, dependency policy, coverage, build, e2e, vuln, containers) and reports a table with evidence, missing tool = not run never passed.
- review: /review [diff|all|path|spec:<name>] [--fix] runs verify quick, launches code-reviewer x3 (backend frontend delivery) + bug-hunter + spec-analyst compliance in parallel, triages every finding itself (confirmed plausible rejected, severity critical high medium low nit), writes report to .reviews/ (git ignored), with --fix fixes confirmed critical high medium with regression tests, never fixes by weakening tests. include checklist and report template references.
- readme: /readme [--check] gathers facts from code and commands, actually starts backend and runs every curl example, writes readme from a template via docs-writer, updates adr index.

**hooks/** shell scripts wired in settings.json + a hooks.conf with switches (GUARDRAILS on, GO_DEPENDENCY_POLICY stdlib-only|no-frameworks|off, PROTECT_CLAUDE_CONFIG on, QUALITY_GATE on with max retries 3, FORMAT_ON_COMMIT on, SUBAGENT_STALE_MINUTES): guard-bash.sh PreToolUse on Bash blocking destructive shell and git (rm -rf outside repo, git push --force, rebase, amend, filter-branch, global git config, killing processes by name, global package installs, go get when stdlib-only, anything that changes the os like defaults/shutdown), guard-files.sh PreToolUse on edits blocking .env files lockfiles generated output .git and the claude config itself and non stdlib go imports per the policy, quality-gate.sh on Stop that when backend or frontend changed this turn runs build vet typecheck lint and unit tests and doesnt let the turn end until they pass (max 3 attempts, skip file to pause, deferred while subagents run), session-context.sh on SessionStart printing a project snapshot (scaffold state, specs and plans, toolchain versions, git state, hook switches), track-changes.sh PostToolUse remembering which side changed, track-subagents.sh on SubagentStart/Stop, format-staged.sh + git-commit-prep.sh so every commit is formatted through a githooks/pre-commit with core.hooksPath. settings.json with permissions allow list for the normal go npm npx make git docker curl commands, ask list for push checkout reset npm install go get etc, deny list for reading env secrets ssh aws and editing settings/hooks/githooks, and the hook wiring.

also make specs/ with a README explaining the workflow and _template.md, and a .claude/.gitignore for the hooks state dir. everything must actually work: the skeleton templates must build lint and pass their tests, ci passes actionlint, dockerfiles pass hadolint, scripts pass shellcheck. keep wording professional in the files even if im not.
